import { CobranzaExternaGuard } from './cobranza-externa.guard';

describe('BancardAuthGuardGuard', () => {
  it('should be defined', () => {
    expect(new CobranzaExternaGuard()).toBeDefined();
  });
});
